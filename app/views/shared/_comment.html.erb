<li>
  <span class="comm"><%= comment.comm %></span> <b />
  <span class="timestamp">
    Posted <%= time_ago_in_words(comment.created_at) %> ago.
  </span>
  <% if current_user?(comment.user) %>
    <%= link_to "delete", comment, method: :delete, data: { confirm: "You sure?" }, title: comment.comm %>
  <% end %>
</li>